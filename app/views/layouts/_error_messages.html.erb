<% if book.errors.any? %>
  <div id="error_explanation">
    <h3 style="color:red;"><%= pluralize(book.errors.count, "error") %> errors prohibited this obj from being saved:</h3>
      <ul>
        <% book.errors.full_messages.each do |message| %>
          <li style="color:red;">
            <%= message %>
          </li>
        <% end %>
      </ul>
  </div>
<% end %>